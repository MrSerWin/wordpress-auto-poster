# 📱 Telegram - Настройка за 5 минут

## Преимущества Telegram

- ✅ **Бесплатно навсегда**
- ✅ Самый простой API из всех соцсетей
- ✅ Токены никогда не истекают
- ✅ Поддержка изображений и форматирования
- ✅ Мгновенная доставка
- ✅ Растущая аудитория (особенно в tech)
- ✅ Отличная альтернатива Facebook/Instagram

## Быстрая настройка

### Шаг 1: Создать Telegram бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте команду:
   ```
   /newbot
   ```
4. Введите имя бота (например: `My Blog Bot`)
5. Введите username бота (например: `myblog_posts_bot`)
6. **Скопируйте токен** (выглядит так: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

✅ Бот создан!

---

### Шаг 2: Создать Telegram канал

1. В Telegram нажмите "Новое сообщение" → "Новый канал"
2. Введите название (например: `My Tech Blog`)
3. Выберите тип: **Публичный канал**
4. Введите username канала (например: `mytechblog`)
5. Создайте канал

✅ Канал создан!

---

### Шаг 3: Добавить бота в канал

1. Откройте ваш канал
2. Нажмите на название канала → **Администраторы**
3. Нажмите **Добавить администратора**
4. Найдите вашего бота (по username)
5. Дайте права: **Публикация сообщений**
6. Сохраните

✅ Бот добавлен как администратор!

---

### Шаг 4: Получить Channel ID

**Вариант А: Использовать username (проще)**

Если ваш канал публичный:
```bash
TELEGRAM_CHANNEL_ID=@mytechblog
```

**Вариант Б: Получить числовой ID**

1. Отправьте любое сообщение в ваш канал
2. Перешлите это сообщение боту @username_to_id_bot
3. Бот ответит с ID канала (например: `-1001234567890`)

Или используйте API:
```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

Найдите в ответе `"chat":{"id":-1001234567890}`

---

### Шаг 5: Добавить в .env

```bash
# Telegram
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHANNEL_ID=@mytechblog
# или
TELEGRAM_CHANNEL_ID=-1001234567890
```

✅ Готово!

---

## Тестирование

### Быстрый тест:

```bash
python -c "from social_media_clients import TelegramPublisher; p = TelegramPublisher(); print('Enabled:', p.is_enabled())"
```

Должно вывести: `Enabled: True`

### Тест публикации:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage?chat_id=@your_channel&text=Test%20message"
```

Или через Python:
```python
import requests

BOT_TOKEN = "your_bot_token"
CHANNEL_ID = "@your_channel"

url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
data = {
    'chat_id': CHANNEL_ID,
    'text': '🧪 Test post from Auto Publisher'
}

response = requests.post(url, data=data)
print(response.json())
```

---

## Публикация с изображением

Telegram автоматически поддерживает изображения:

```python
url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendPhoto"

with open('image.png', 'rb') as photo:
    files = {'photo': photo}
    data = {
        'chat_id': CHANNEL_ID,
        'caption': 'Текст поста'
    }
    response = requests.post(url, files=files, data=data)
```

---

## Форматирование текста

Telegram поддерживает **Markdown**:

```python
text = """
📰 **Новая статья**

_Описание статьи в курсиве_

**Жирный текст**

`code`

[Ссылка](https://example.com)

#hashtag #AI #Tech
"""

data = {
    'chat_id': CHANNEL_ID,
    'text': text,
    'parse_mode': 'Markdown'
}
```

---

## Использование

После настройки публикация происходит автоматически:

```bash
python auto_publisher.py --publish-now
```

Результат:
```
[Telegram] ✅ Published: 123
```

Пост появится в вашем Telegram канале!

---

## Troubleshooting

### "Chat not found"
- Убедитесь что бот добавлен как администратор канала
- Проверьте правильность channel ID или username

### "Bot was blocked by the user"
- Канал должен быть публичным или бот должен быть администратором

### "Bad Request: message text is empty"
- Проверьте что текст не пустой
- Telegram требует минимум 1 символ

### "Photo required"
- При использовании sendPhoto убедитесь что файл существует

---

## Дополнительные возможности

### Кнопки под постом:

```python
data = {
    'chat_id': CHANNEL_ID,
    'text': 'Текст поста',
    'reply_markup': {
        'inline_keyboard': [[
            {'text': 'Читать полностью', 'url': 'https://yoursite.com/article'}
        ]]
    }
}
```

### Пины важных постов:

```python
# После публикации
message_id = result['result']['message_id']

# Запинить
requests.post(
    f"https://api.telegram.org/bot{BOT_TOKEN}/pinChatMessage",
    data={'chat_id': CHANNEL_ID, 'message_id': message_id}
)
```

### Планирование отправки:

Telegram поддерживает отложенную публикацию!

---

## Продвижение канала

После настройки:

1. **Добавьте описание канала** с ключевыми словами
2. **Установите аватар** для канала
3. **Добавьте ссылку на канал** на ваш сайт
4. **Кросс-промо** в других соцсетях
5. **Регулярные публикации** - Telegram любит активные каналы

---

## Аналитика

Просмотры постов:
```python
url = f"https://api.telegram.org/bot{BOT_TOKEN}/getChat"
response = requests.get(url, params={'chat_id': CHANNEL_ID})
print(response.json())
```

---

## Сравнение с Facebook/Instagram

| Критерий | Telegram | Facebook | Instagram |
|----------|----------|----------|-----------|
| Настройка | 5 минут | 30+ минут | 30+ минут |
| Токены | Вечные | Истекают | Истекают |
| Изображения | ✅ Просто | ✅ | ⚠️ Требует URL |
| Форматирование | ✅ Markdown | ⚠️ Ограниченно | ❌ |
| API сложность | Очень просто | Сложно | Очень сложно |
| Стоимость | Бесплатно | Бесплатно | Бесплатно |

---

## Итоговая рекомендация

**Используйте связку:**
- ✅ **VK** - для русскоязычной аудитории (уже работает!)
- ✅ **Twitter** - для международной аудитории (уже работает!)
- ✅ **Telegram** - для tech-аудитории (настроить за 5 минут!)

**3 платформы = отлично!**

Facebook, Instagram и Threads можно пропустить из-за сложности.

---

## Готово! 🎉

Telegram - самая простая и надёжная платформа для автопостинга!

Теперь ваши статьи будут автоматически публиковаться в **VK + Twitter + Telegram**!
