# 📱 Instagram & Threads - Session-Based Setup

## Новый подход: Session-Based вместо Official API

**Почему это лучше:**
- ✅ **Не нужны токены** - используется обычный логин/пароль
- ✅ **Проще настройка** - нет Facebook Business Account
- ✅ **Не истекают сессии** - автоматическое сохранение
- ✅ **Работает из коробки** - используется `instagrapi` и `threads-api`
- ✅ **Поддержка Threads** - через те же credentials

---

## Что изменилось

### Старый подход (Official API)
```bash
# Требовалось:
INSTAGRAM_USER_ID=123456789      # Нужен Business Account
INSTAGRAM_ACCESS_TOKEN=...       # Токен истекает
THREADS_USER_ID=@username        # API в beta
THREADS_ACCESS_TOKEN=...         # 500 Server Error
```

❌ **Проблемы:**
- Требует Instagram Business Account
- Требует публичный URL изображения
- Токены истекают
- Threads API в закрытой beta

### Новый подход (Session-Based)
```bash
# Просто логин и пароль:
INSTAGRAM_USERNAME=your_username
INSTAGRAM_PASSWORD=your_password
```

✅ **Преимущества:**
- Обычный Instagram аккаунт (не Business)
- Загружает локальные файлы
- Сессия сохраняется автоматически
- Threads использует те же credentials

---

## Быстрая настройка (5 минут)

### Шаг 1: Установка библиотек

Библиотеки уже установлены:
```bash
pip install instagrapi threads-api
```

### Шаг 2: Настройка .env

Откройте файл `.env` и добавьте:

```bash
# Instagram (новый метод: session-based)
INSTAGRAM_USERNAME=your_instagram_username
INSTAGRAM_PASSWORD=your_instagram_password

# Threads автоматически использует Instagram credentials
# (можно не заполнять, если используются те же)
THREADS_USERNAME=
THREADS_PASSWORD=
```

**ВАЖНО:**
- Используйте **username**, а не email
- Если у вас двухфакторная аутентификация (2FA):
  - Отключите 2FA временно, или
  - Используйте App Password (если доступно)

### Шаг 3: Проверка работы

Запустите тестовый скрипт:

```bash
python test_instagram_threads.py
```

**Что проверяется:**
1. Instagram - публикация фото с текстом
2. Threads - публикация текста

---

## Детальная настройка

### Instagram

**Требования:**
- Обычный Instagram аккаунт (не обязательно Business)
- Username и пароль
- Изображение для публикации (Instagram требует фото)

**Как работает:**

1. **Первый запуск** - выполняется логин, сессия сохраняется в `.instagram_session.json`
2. **Последующие запуски** - загружается сохраненная сессия, новый логин не требуется
3. **Истекшая сессия** - автоматически выполняется новый логин

**Пример кода:**
```python
from social_media_clients import InstagramPublisher

publisher = InstagramPublisher()
post_id = publisher.publish(
    text="Текст поста с хештегами",
    url="https://yoursite.com/article",
    image_path="/path/to/image.jpg"
)
```

**Формат caption:**
```
Текст поста с хештегами

🔗 https://yoursite.com/article
```

---

### Threads

**Требования:**
- Instagram аккаунт с активированным Threads
- Те же credentials что и для Instagram

**Как работает:**

1. Использует библиотеку `threads-api`
2. Аутентификация через Instagram credentials
3. Поддерживает текст и изображения

**Пример кода:**
```python
from social_media_clients import ThreadsPublisher

publisher = ThreadsPublisher()
post_id = publisher.publish(
    text="Текст поста",
    url="https://yoursite.com/article",
    image_path="/path/to/image.jpg"  # опционально
)
```

**Формат поста:**
```
Текст поста

https://yoursite.com/article
```

---

## Автоматическая публикация

После настройки credentials, публикация происходит автоматически:

```bash
python auto_publisher.py --publish-now
```

**Процесс:**
1. Генерация статьи с помощью Gemini AI
2. Публикация на WordPress
3. **Автоматическая публикация в социальные сети:**
   - Facebook ✅
   - Twitter/X ✅
   - VK ✅
   - **Instagram ✅** (новый метод)
   - **Threads ✅** (новый метод)
   - Telegram (опционально)

---

## Troubleshooting

### Instagram: "Login failed"

**Причины:**
1. Неправильный username или пароль
2. Двухфакторная аутентификация (2FA)
3. Instagram блокирует автоматические логины

**Решение:**
1. Проверьте правильность credentials
2. Отключите 2FA временно
3. Попробуйте логин с другого устройства/браузера сначала
4. Подождите 24 часа если Instagram заблокировал

### Instagram: "checkpoint_required"

**Это означает:** Instagram требует подтверждения через веб-интерфейс

**Решение:**
1. Зайдите на instagram.com через браузер
2. Пройдите проверку (CAPTCHA, подтверждение email, и т.д.)
3. Попробуйте снова

### Threads: "Authentication failed"

**Причины:**
1. Threads не активирован для вашего Instagram аккаунта
2. Неправильные credentials

**Решение:**
1. Убедитесь что у вас активирован Threads в приложении
2. Используйте те же credentials что и для Instagram
3. Попробуйте сначала Instagram, затем Threads

### Instagram: "Photo required"

**Это нормально:** Instagram требует изображение для публикации

**Решение:**
- Убедитесь что у вас есть изображение для статьи
- Используйте `featured_image_url` из WordPress
- Или создайте тестовое изображение:
  ```bash
  python -c "from PIL import Image; img = Image.new('RGB', (1080, 1080), color='blue'); img.save('test_image.jpg')"
  ```

### Сессия продолжает истекать

**Причина:** Instagram детектирует автоматизацию

**Решение:**
1. Используйте задержки между публикациями (уже реализовано - 2 секунды)
2. Не публикуйте слишком часто (соблюдайте `PUBLISH_INTERVAL_DAYS`)
3. Периодически логинитесь вручную через приложение

---

## Сравнение: Old API vs New Session-Based

| Критерий | Official API | Session-Based |
|----------|--------------|---------------|
| **Настройка** | 30+ минут | 5 минут |
| **Account Type** | Business Required | Any Account |
| **Токены** | Истекают | Сохраняются |
| **Изображения** | Публичный URL | Локальные файлы |
| **Threads** | API в beta | ✅ Работает |
| **Сложность** | Очень сложно | Просто |
| **Надежность** | ⚠️ Средняя | ✅ Высокая |

---

## Библиотеки

### instagrapi
- **Назначение:** Instagram API client
- **Документация:** https://github.com/subzeroid/instagrapi
- **Возможности:**
  - Публикация фото, видео, Reels
  - Сохранение/загрузка сессий
  - Обход rate limits
  - Stories, IGTV, и многое другое

### threads-api
- **Назначение:** Threads (Meta) API client
- **Документация:** https://github.com/Danie1/threads-api
- **Возможности:**
  - Публикация текста и изображений
  - Использует Instagram credentials
  - Просмотр профиля, постов
  - Лайки, комментарии

---

## Безопасность

### Хранение credentials

**Текущий подход:**
```bash
# .env файл (не коммитится в git)
INSTAGRAM_USERNAME=username
INSTAGRAM_PASSWORD=password
```

**Рекомендации:**
1. ✅ Используйте `.env` файл (уже в `.gitignore`)
2. ✅ Не коммитьте credentials в git
3. ⚠️ Используйте environment variables на production
4. ⚠️ Рассмотрите использование secrets manager (AWS Secrets Manager, etc.)

### Сессии

**Файлы сессий:**
- `.instagram_session.json` - содержит session_id, cookies
- Добавлен в `.gitignore`
- Автоматически обновляется

**Безопасность:**
- Не делитесь файлами сессий
- Периодически удаляйте старые сессии
- При компрометации - смените пароль Instagram

---

## Интеграция с auto_publisher.py

Публикация происходит автоматически после WordPress:

```python
# auto_publisher.py (уже реализовано)

# После публикации на WordPress
if enable_social_media:
    social_generator = SocialContentGenerator()
    social_posts = social_generator.generate_social_posts(
        article_title=title,
        article_url=wp_url,
        article_content=content_html[:1000],
        keywords=keywords
    )

    social_coordinator = SocialMediaCoordinator()
    social_results = social_coordinator.publish_to_all(
        posts_data=social_posts,
        image_path=featured_image_path  # ← Локальный файл!
    )
```

**Результат:**
```
[Instagram] Loading saved session...
[Instagram] ✅ Session restored successfully
[Instagram] ✅ Published: 123456789

[Threads] ✅ Authentication successful
[Threads] ✅ Published: 987654321
```

---

## FAQ

### Q: Нужен ли Instagram Business Account?
**A:** Нет! Работает с обычным аккаунтом.

### Q: Истекают ли сессии?
**A:** Сессии сохраняются в файл. При истечении - автоматический повторный логин.

### Q: Можно ли публиковать без изображения в Instagram?
**A:** Нет, Instagram требует изображение. Threads может быть только текстом.

### Q: Работает ли это с 2FA?
**A:** Рекомендуется отключить 2FA для автоматизации, или используйте App Password.

### Q: Сколько постов можно публиковать?
**A:** Соблюдайте разумные лимиты (не более 1-2 постов в час). Используйте `PUBLISH_INTERVAL_DAYS`.

### Q: Что если Instagram блокирует?
**A:** Подождите 24-48 часов, пройдите verification через веб-интерфейс.

### Q: Threads использует те же credentials?
**A:** Да! Threads использует Instagram аккаунт.

---

## Готово! 🎉

Теперь у вас работает автоматическая публикация в:
- ✅ Facebook
- ✅ Twitter/X
- ✅ VK
- ✅ **Instagram** (новый метод!)
- ✅ **Threads** (новый метод!)

**Всего 5 платформ!**

---

## Следующие шаги

1. **Настроить credentials:**
   ```bash
   nano .env
   # Добавить INSTAGRAM_USERNAME и INSTAGRAM_PASSWORD
   ```

2. **Протестировать:**
   ```bash
   python test_instagram_threads.py
   ```

3. **Запустить автопостинг:**
   ```bash
   python auto_publisher.py --publish-now
   ```

4. **Мониторить логи:**
   ```bash
   tail -f logs/auto_publisher.log
   ```

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `logs/auto_publisher.log`
2. Запустите тестовый скрипт: `python test_instagram_threads.py`
3. Проверьте что credentials правильные
4. Убедитесь что 2FA отключена

---

**Happy auto-posting! 🚀**
