# Настройка публикации в социальные сети

Автоматическая публикация статей в Facebook, Twitter/X, Threads, VK и Instagram после публикации на WordPress.

## Возможности

- ✅ Автоматическая генерация кратких саммари статей с помощью Gemini AI
- ✅ Оптимизированные тексты для каждой платформы
- ✅ Автоматическая генерация релевантных хештегов
- ✅ Публикация во все настроенные социальные сети
- ✅ Гибкая настройка - включайте только нужные платформы
- ✅ Детальное логирование результатов публикаций

## Быстрый старт

### 1. Включить публикацию в социальные сети

В `.env` файле:
```bash
ENABLE_SOCIAL_MEDIA=true
```

### 2. Настроить API ключи

Добавьте ключи для нужных платформ в `.env` (см. инструкции ниже).

### 3. Публикация

Система автоматически публикует в социальные сети после публикации на WordPress:
```bash
python auto_publisher.py --publish-now
```

---

## Настройка каждой платформы

### 📘 Facebook

#### Что нужно:
- Facebook Page (не личный профиль)
- Facebook App
- Page Access Token

#### ⚠️ ВАЖНО: Если вы видите "No businesses available"

Это означает, что:
1. У вас нет Facebook Page
2. Или у вас нет доступа к Business Manager

**Решение:**

**ВАРИАНТ А: Создать Facebook Page (Рекомендуется для начала)**

1. **Создать Facebook Page**:
   - Откройте https://www.facebook.com/pages/create
   - Выберите категорию вашей страницы
   - Заполните название и категорию
   - Создайте страницу

2. **Получить Page ID**:
   - Откройте вашу новую страницу
   - Нажмите "About" на левой панели
   - Прокрутите вниз → найдите "Page ID" и скопируйте

**ВАРИАНТ Б: Упрощённая настройка через Graph API Explorer**

1. **Открыть Graph API Explorer**:
   - Перейдите: https://developers.facebook.com/tools/explorer/

2. **Создать Page Access Token**:
   - В поле "Meta App" выберите "Graph API Explorer" (встроенное приложение)
   - Нажмите "Generate Access Token"
   - В появившемся окне дайте разрешения:
     - `pages_manage_posts` (публикация от имени страницы)
     - `pages_read_engagement` (чтение метрик)
   - **ВАЖНО**: Вас попросят выбрать Page - выберите вашу страницу
   - Скопируйте сгенерированный токен

3. **Получить Page ID**:
   - В Graph API Explorer введите `me/accounts` в поле запроса
   - Нажмите "Submit"
   - Найдите вашу страницу в ответе и скопируйте её `id`

**ВАРИАНТ В: Полная настройка с собственным приложением**

Только если вам нужен долгосрочный токен (60+ дней):

1. **Создать Facebook App**:
   - Откройте https://developers.facebook.com/apps
   - Нажмите "Create App"
   - Выберите тип: **"Business"** (или "Other" если нет Business)
   - Заполните:
     - App Name: "My Auto Poster"
     - App Contact Email: ваш email
   - Нажмите "Create App"

2. **Настроить Facebook Login**:
   - В дашборде приложения → "Add Product"
   - Найдите "Facebook Login" → "Set Up"
   - Выберите "Web" → введите URL: `http://localhost`
   - Сохраните

3. **Получить долгосрочный токен**:
   - Перейдите в "Tools" → "Access Token Tool"
   - Или используйте Graph API Explorer
   - Выберите ваше приложение
   - Получите User Access Token с разрешениями:
     - `pages_manage_posts`
     - `pages_read_engagement`
   - Обменяйте на долгосрочный токен (60 дней):
     ```bash
     curl "https://graph.facebook.com/v18.0/oauth/access_token?grant_type=fb_exchange_token&client_id=YOUR_APP_ID&client_secret=YOUR_APP_SECRET&fb_exchange_token=SHORT_LIVED_TOKEN"
     ```

4. **Добавить в `.env`**:
```bash
FACEBOOK_PAGE_ID=ваш_page_id
FACEBOOK_ACCESS_TOKEN=ваш_токен
```

#### Тестирование:
```bash
python -c "from social_media_clients import FacebookPublisher; p = FacebookPublisher(); print('Enabled:', p.is_enabled())"
```

#### 🆘 Если не работает:

1. **"Invalid OAuth access token"**
   - Токен истёк → получите новый в Graph API Explorer
   - Проверьте что токен именно для **Page**, а не User

2. **"No businesses available"**
   - Создайте Facebook Page сначала (Вариант А)
   - Используйте встроенное приложение "Graph API Explorer" (Вариант Б)

3. **"This app is in Development Mode"**
   - Добавьте себя как Test User
   - Или переключите приложение в Live Mode (требует проверку)

#### 💡 Рекомендация:

**Для начала используйте ВАРИАНТ Б** - самый быстрый способ:
1. Создайте Facebook Page
2. Используйте Graph API Explorer для получения токена
3. Токен будет работать ~2 часа (для тестирования достаточно)
4. Позже можете настроить долгосрочный токен (ВАРИАНТ В)

---

### 🐦 Twitter/X

#### Что нужно:
- Twitter Developer Account
- Twitter App
- API Keys и Access Tokens

#### Шаги настройки:

1. **Создать Developer Account**:
   - Откройте https://developer.twitter.com/
   - Подайте заявку на Developer Account
   - Дождитесь одобрения (обычно 1-3 дня)

2. **Создать приложение**:
   - В Developer Portal → "Projects & Apps"
   - Создайте новый Project и App
   - Выберите "Read and Write" permissions

3. **Получить ключи**:
   - В настройках App → "Keys and tokens"
   - Сгенерируйте:
     - API Key и API Secret
     - Access Token и Access Secret
     - Bearer Token

4. **Добавить в `.env`**:
```bash
TWITTER_API_KEY=ваш_api_key
TWITTER_API_SECRET=ваш_api_secret
TWITTER_ACCESS_TOKEN=ваш_access_token
TWITTER_ACCESS_SECRET=ваш_access_secret
TWITTER_BEARER_TOKEN=ваш_bearer_token
```

5. **Установить tweepy**:
```bash
pip install tweepy
```

#### Тестирование:
```bash
python -c "from social_media_clients import TwitterPublisher; p = TwitterPublisher(); print('Enabled:', p.is_enabled())"
```

---

### 🧵 Threads

#### Что нужно:
- Threads Professional Account
- Instagram Business Account (Threads использует Instagram API)
- Facebook App

#### Шаги настройки:

1. **Настроить Instagram Business**:
   - Переключите ваш Instagram на Business Account
   - Подключите к Facebook Page

2. **Настроить Threads API**:
   - Используйте тот же процесс что и для Instagram
   - Threads API использует Instagram Graph API

3. **Получить токены**:
   - Откройте https://developers.facebook.com/
   - В Graph API Explorer выберите Instagram Account
   - Дайте разрешения: `threads_basic`, `threads_content_publish`

4. **Добавить в `.env`**:
```bash
THREADS_USER_ID=ваш_instagram_user_id
THREADS_ACCESS_TOKEN=ваш_токен
```

**Примечание**: Threads API пока в beta, может требовать дополнительную регистрацию.

---

### 🔵 VK (ВКонтакте)

#### Что нужно:
- VK Group (сообщество)
- VK Access Token

#### Шаги настройки:

1. **Создать Standalone приложение**:
   - Откройте https://vk.com/apps?act=manage
   - Создайте "Standalone приложение"
   - Сохраните App ID

2. **Получить Access Token для сообщества**:
   - Откройте ваше сообщество → "Управление"
   - Перейдите в "Настройки" → "Работа с API"
   - Создайте ключ доступа
   - Дайте разрешение: "Управление сообществом"

3. **Получить Group ID**:
   - В адресной строке вашего сообщества: `vk.com/public12345678`
   - Group ID = `12345678` (без "public" и минуса)

4. **Добавить в `.env`**:
```bash
VK_ACCESS_TOKEN=ваш_токен
VK_GROUP_ID=12345678
```

#### Тестирование:
```bash
python -c "from social_media_clients import VKPublisher; p = VKPublisher(); print('Enabled:', p.is_enabled())"
```

---

### 📸 Instagram

#### Что нужно:
- Instagram Business Account
- Facebook Page (связанная с Instagram)
- Facebook App
- Публичный URL изображения

#### Шаги настройки:

1. **Подготовка аккаунта**:
   - Переключите Instagram на Business Account
   - Подключите к Facebook Page

2. **Создать Facebook App**:
   - https://developers.facebook.com/apps
   - Добавьте продукт "Instagram Graph API"

3. **Получить токены**:
   - Graph API Explorer → выберите ваш Instagram Business Account
   - Дайте разрешения: `instagram_basic`, `instagram_content_publish`, `pages_read_engagement`
   - Скопируйте User ID и Access Token

4. **Добавить в `.env`**:
```bash
INSTAGRAM_USER_ID=ваш_instagram_business_id
INSTAGRAM_ACCESS_TOKEN=ваш_токен
```

**Важно**: Instagram требует публичный URL изображения. Убедитесь, что ваши изображения доступны по HTTP.

#### Тестирование:
```bash
python -c "from social_media_clients import InstagramPublisher; p = InstagramPublisher(); print('Enabled:', p.is_enabled())"
```

---

## Использование

### Автоматическая публикация

После настройки API ключей, публикация происходит автоматически:

```bash
# Опубликовать следующую статью
python auto_publisher.py --publish-now

# Запустить автоматический режим (публикация каждые N дней)
python auto_publisher.py --daemon
```

### Проверка статуса

```bash
python auto_publisher.py --status
```

### Тестирование только социальных сетей

```bash
# Тестировать генерацию контента
python social_content_generator.py

# Тестировать публикацию (с вашими API ключами)
python social_media_clients.py
```

---

## Как работает

### 1. Генерация контента

После публикации на WordPress, система использует Gemini AI для создания оптимизированного контента для каждой платформы:

- **Facebook**: 200-250 символов, профессиональный тон, призыв к действию
- **Twitter/X**: До 270 символов, краткий и ёмкий
- **Threads**: 300-400 символов, разговорный стиль
- **VK**: 200-300 символов, для русскоязычной аудитории
- **Instagram**: 150-200 символов, визуально-ориентированный

### 2. Хештеги

Автоматически генерируются релевантные хештеги:
- Популярные и нишевые
- Оптимизированные для каждой платформы
- На основе ключевых слов статьи

### 3. Публикация

Посты публикуются последовательно во все настроенные платформы с задержкой 2 секунды между публикациями.

---

## Отключение социальных сетей

### Отключить все платформы:

```bash
ENABLE_SOCIAL_MEDIA=false
```

### Отключить отдельные платформы:

Просто не указывайте API ключи для ненужных платформ в `.env`.

---

## Логирование

Все публикации логируются в `auto_publisher.log`:

```
✅ Статья опубликована в WordPress: Title -> WP ID: 123
📎 URL: https://yoursite.com/article-slug
📱 Начинаем публикацию в социальные сети...
📱 Социальные сети: 3/5 публикаций успешны
   ✅ facebook: 123456789
   ✅ twitter: 987654321
   ✅ vk: 456789123
   ⚠️  threads: not_configured
   ⚠️  instagram: not_configured
✅ Публикация завершена: Title
```

---

## Troubleshooting

### Facebook: "Invalid OAuth access token"
- Проверьте срок действия токена
- Обновите токен в Graph API Explorer
- Убедитесь, что у токена есть права `pages_manage_posts`

### Twitter: "403 Forbidden"
- Проверьте что у приложения есть "Read and Write" permissions
- Regenerate access tokens после изменения permissions
- Установите `tweepy`: `pip install tweepy`

### VK: "Access denied"
- Убедитесь что токен создан для сообщества (не пользователя)
- Проверьте что включено "Управление сообществом"
- Group ID должен быть без минуса

### Instagram: "Media creation failed"
- Instagram требует публичный URL изображения
- Убедитесь что изображение доступно по HTTPS
- Размер изображения должен быть 1:1 или 4:5

### Threads: API недоступен
- Threads API пока в закрытой beta
- Возможно потребуется заявка на доступ

---

## Безопасность

⚠️ **Важно**:
- Никогда не коммитьте `.env` файл в Git
- Храните API ключи в безопасности
- Используйте токены с минимальными необходимыми правами
- Регулярно обновляйте токены

---

## Требования

```bash
pip install requests python-dotenv tweepy pillow google-generativeai
```

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `tail -f auto_publisher.log`
2. Протестируйте каждую платформу отдельно
3. Убедитесь что API ключи валидны
4. Проверьте квоты API

---

## Следующие шаги

После настройки:
1. Протестируйте публикацию: `python auto_publisher.py --publish-now`
2. Проверьте социальные сети на наличие постов
3. Настройте автоматический режим для регулярных публикаций
4. Мониторьте логи для отслеживания успешности публикаций
