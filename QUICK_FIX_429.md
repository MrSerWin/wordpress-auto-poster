# ⚡ Быстрое исправление ошибки 429

## 🔴 Проблема
```
HTTP/1.1 429 Too Many Requests
Quota exceeded for metric: generativelanguage.googleapis.com/generate_content_free_tier_requests
```

## ✅ Решение за 3 шага

### Шаг 1: Проверьте квоту
```bash
python check_quota.py
```

Это покажет:
- ✅ Доступна ли квота
- ⏰ Нужно ли подождать
- 💡 Конкретные действия

---

### Шаг 2: Дождитесь сброса квоты

**Бесплатный tier Gemini**:
- ⏰ **RPM (requests per minute)**: 15 запросов/минуту → сбрасывается **каждую минуту**
- 📅 **RPD (requests per day)**: 1,500 запросов/день → сбрасывается **в полночь UTC**

**Рекомендация**: Подождите **1-2 минуты** и попробуйте снова.

```bash
# Подождите 2 минуты, затем:
python check_quota.py
```

---

### Шаг 3: Публикуйте статью

Когда квота доступна:
```bash
python auto_publisher.py --publish-now
```

**Теперь код автоматически**:
- ✅ Делает задержки между запросами (2 сек)
- ✅ Ждет столько, сколько рекомендует API
- ✅ Повторяет запрос при ошибке 429
- ✅ Показывает понятные сообщения

---

## 📊 Мониторинг

### Проверить использование квоты:
https://ai.dev/usage?tab=rate-limit

### Проверить статус публикаций:
```bash
python auto_publisher.py --status
```

### Посмотреть логи:
```bash
tail -f auto_publisher.log
```

---

## 🚨 Если проблема не решена

### 1. Проверьте запущенные процессы
```bash
ps aux | grep auto_publisher
# Убейте старые процессы:
kill -9 <PID>
```

### 2. Убедитесь в единственном использовании API ключа
- Проверьте нет ли других приложений/скриптов с тем же ключом
- Проверьте нет ли запущенных тестов

### 3. Увеличьте интервал публикации

Откройте [auto_publisher.py:35](auto_publisher.py#L35):
```python
PUBLISH_INTERVAL_DAYS = 7  # Было 3, стало 7 дней
```

### 4. Создайте новый API ключ

1. Откройте: https://aistudio.google.com/app/apikey
2. Создайте новый ключ
3. Обновите `.env`:
   ```
   GOOGLE_API_KEY=ваш_новый_ключ
   ```

### 5. Перейдите на платный план

**Преимущества**:
- 🚀 1,000 RPM (вместо 15)
- 📈 10,000 RPD (вместо 1,500)
- 💰 Pay-as-you-go

**Как**:
1. Откройте: https://console.cloud.google.com/billing
2. Включите биллинг
3. Создайте новый API ключ из проекта с биллингом

**Цены**: https://ai.google.dev/pricing

---

## 📚 Подробная документация

См. [RATE_LIMITING_FIX.md](RATE_LIMITING_FIX.md) для полного объяснения всех изменений.

---

## ✅ Проверка после исправления

```bash
# 1. Проверьте квоту
python check_quota.py

# 2. Если квота доступна - опубликуйте тестовую статью
python auto_publisher.py --publish-now

# 3. Проверьте логи
tail -n 50 auto_publisher.log
```

**Ожидаемый результат**:
```
[gemini_client] Initialized with Gemini API
[gemini_client] Rate limiting: waiting 2.0s before next request
✅ Article generated...
[gemini_client] Waiting 3 seconds before generating image...
✅ Image generated successfully
✅ Статья опубликована: ...
```

---

## 🆘 Нужна помощь?

- 📖 [Документация Gemini API](https://ai.google.dev/gemini-api/docs)
- 💬 [Stack Overflow](https://stackoverflow.com/questions/tagged/google-gemini)
- 🐛 [GitHub Issues](https://github.com/google-gemini/generative-ai-python/issues)
